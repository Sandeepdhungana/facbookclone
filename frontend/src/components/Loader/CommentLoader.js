import React from "react";
import Loader from "react-js-loader";

const CommentLoader = () => {
  return (
    <div>
      <Loader type="spinner-default" bgColor={"#1877F2"} size={40} />
    </div>
  );
};

export default CommentLoader;
